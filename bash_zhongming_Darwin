if [ -d ~/.zhongming_completion ]; then
    for file in ~/.zhongming_completion/*-completion.bash; do
        . $file
    done
fi

export PATH=~/bin:~/opt/bin:$PATH

export PS1="\\w:\$(git branch 2>/dev/null | grep '^*' | colrm 1 2)\$ "

alias eb="gvim ~/.bash_zhongming"
alias sb="source ~/.bash_zhongming"

alias ..="cd .."
alias l="ls -GF"
alias ls="ls -aGF"
alias ll="ls -alGhF"
alias ctagscpp="ctags --c++-kinds=+p --fields=+iaS --extra=+q"

# For Android Studio
export ANDROID_NDK_HOME=~/bin/android-ndk-r10d
launchctl setenv ANDROID_NDK_HOME "$ANDROID_NDK_HOME"

export ANDROID_HOME=~/Library/Android/sdk
launchctl setenv ANDROID_HOME "$ANDROID_HOME"

export PATH=$ANDROID_NDK_HOME/tools:$ANDROID_HOME/platform-tools:$PATH

# For Fleksy
export FLEKSY_JET_ROOT=~/git/jet-files/jets

# Unmount evething in ~/dev/
alias unmountalldev="find ~/dev -type d | xargs umount -f"

# ICU paths
export LD_LIBRARY_PATH=/opt/icu4c/56.1/lib:$LD_LIBRARY_PATH
export PKG_CONFIG_PATH=/opt/icu4c/56.1/lib/pkgconfig:$PKG_CONFIG_PATH
